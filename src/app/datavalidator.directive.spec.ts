import { DatavalidatorDirective } from './datavalidator.directive';

describe('DatavalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DatavalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
